<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Botón de Salida</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
        }
        .button-container {
            display: flex;
            justify-content: flex-start;   /* Alinea el contenido al inicio (izquierda) */
            align-items: flex-start;       /* Alinea el contenido al inicio (arriba) */
            padding: 5px;                 /* Espacio desde los bordes */
        }
        button {
            padding: 10px 20px;            /* Espacio interior del botón */
            font-size: 20px;               /* Tamaño de la fuente */
            background-color: #900C3F;     /* Color de fondo */
            color: #45b39d;                /* Color del texto */
            border: none;                  /* Sin borde */
            border-radius: 5px;            /* Bordes redondeados */
            cursor: pointer;               /* Cambia el cursor al pasar por encima */
            transition: background-color 0.3s ease; /* Transición suave para el fondo */
        }
        button:hover {
            background-color: #70092D;     /* Color de fondo al pasar el cursor */
        }
        button:focus {
            outline: none;                 /* Sin borde de enfoque */
        }
    </style>
</head>
<body>

</body>
</html>

